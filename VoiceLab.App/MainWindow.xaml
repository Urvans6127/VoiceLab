<Window x:Class="VoiceLab.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{DynamicResource App.Title}" Width="1180" Height="800" MinWidth="900" MinHeight="650" Icon="Assets/app.ico"
        WindowStyle="None">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
  </shell:WindowChrome.WindowChrome>
  <Grid Background="{StaticResource PageBackgroundBrush}">
    <Grid.RowDefinitions><RowDefinition Height="44"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <Border Grid.Row="0" Background="{StaticResource SidebarBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1" Padding="0,3,0,1">
      <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
          <Image Source="Assets/app.ico" Width="18" Height="18" Margin="0,0,9,0"/>
          <TextBlock Text="{DynamicResource App.Title}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" shell:WindowChrome.IsHitTestVisibleInChrome="True">
          <Button Style="{StaticResource CaptionButtonStyle}" Content="&#xE921;" Click="MinimizeButton_Click" ToolTip="{DynamicResource Window.Minimize}"/>
          <Button x:Name="MaximizeRestoreButton" Style="{StaticResource CaptionButtonStyle}" Content="&#xE922;" Click="MaximizeRestoreButton_Click" ToolTip="{DynamicResource Window.Maximize}"/>
          <Button Style="{StaticResource CloseCaptionButtonStyle}" Content="&#xE8BB;" Click="CloseButton_Click" ToolTip="{DynamicResource Window.Close}"/>
        </StackPanel>
      </Grid>
    </Border>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions><ColumnDefinition Width="232"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Border Background="{StaticResource SidebarBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,1,0" Padding="14">
        <DockPanel>
          <StackPanel DockPanel.Dock="Top" Margin="8,12,8,26">
            <TextBlock Text="{DynamicResource App.BrandLine1}" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock Text="{DynamicResource App.BrandLine2}" Foreground="{StaticResource AccentBrush}" FontSize="22" FontWeight="SemiBold"/>
          </StackPanel>
          <ListBox ItemsSource="{Binding Items, Mode=OneWay}" SelectedItem="{Binding SelectedItem}" ItemContainerStyle="{StaticResource NavigationItemStyle}" BorderThickness="0" Background="Transparent" KeyboardNavigation.TabNavigation="Continue">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition/></Grid.ColumnDefinitions>
                  <TextBlock Text="{Binding Glyph, Mode=OneWay}" FontFamily="Segoe UI Symbol" Foreground="{StaticResource SecondaryTextBrush}" FontSize="15" VerticalAlignment="Center"/>
                  <TextBlock Grid.Column="1" Text="{Binding Title, Mode=OneWay}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </Grid>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>
      </Border>
      <ContentControl Grid.Column="1" Content="{Binding CurrentPage, Mode=OneWay}" Margin="28,28,28,0" KeyboardNavigation.TabNavigation="Continue"/>
    </Grid>
  </Grid>
</Window>
